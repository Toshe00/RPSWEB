/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["picsum.photos"], // Allow Lorem Picsum images
      },
      devIndicators: false
};

export default nextConfig;
